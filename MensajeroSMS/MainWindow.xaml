<Window x:Class="MensajeroSMS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Mensajero SMS" Height="350" Width="525">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>

        <Grid>
            <Button Content="Cargar datos de Excel" Margin="32,24,0,0" VerticalAlignment="Top" 
                    Click="Load_Contacts_Click" Height="28" 
                    HorizontalAlignment="Left" 
                    Width="151"/>

            
            
            <Button Content="Opciones" Margin="0,24,22,0" VerticalAlignment="Top" Height="28" HorizontalAlignment="Right" Width="117"/>

        </Grid>
        
        <TextBox Grid.Row="1"
                   Text="{Binding Message}"
                   Height="60">
            
        </TextBox>
        
        <Grid
            Grid.Row="2">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <DataGrid 
                AutoGenerateColumns="False" 
                CanUserResizeColumns="True" 
                CanUserAddRows="False" 
                CanUserSortColumns="True" 
                ItemsSource="{Binding Contacts}">

                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Binding="{Binding Selected, UpdateSourceTrigger=LostFocus}">
                        <DataGridCheckBoxColumn.Header>
                            <CheckBox Name="chkSelectAll" IsChecked="true" Checked="chkSelectAll_Checked" Unchecked="chkSelectAll_Unchecked"></CheckBox>
                        </DataGridCheckBoxColumn.Header>
                    </DataGridCheckBoxColumn>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding Name, UpdateSourceTrigger=LostFocus}" />
                    <DataGridTextColumn Header="Celular" Binding="{Binding Cellphone, UpdateSourceTrigger=LostFocus}" />
                </DataGrid.Columns>
            </DataGrid>
            
            <StackPanel 
                Orientation="Horizontal"
                Grid.Row="1">

                <Button Content="Enviar" Click="Send_Click"/>

            </StackPanel>
            
        </Grid>

        <StatusBar Grid.Row="3">

            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem>
                
            </StatusBarItem>
            <Separator Grid.Column="1" />
            <StatusBarItem Grid.Column="2">
                <TextBlock Text="{Binding Properties.CurrentFile, Mode=OneWay}" />
            </StatusBarItem>
            <Separator Grid.Column="3" />
            <StatusBarItem Grid.Column="4">
                <ProgressBar Value="50" Width="90" Height="16" />
            </StatusBarItem>

        </StatusBar>

    </Grid>
</Window>
